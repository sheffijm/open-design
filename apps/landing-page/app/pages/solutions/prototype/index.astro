---
/*
 * /solutions/prototype/ — Use-case page: prototyping with Open Design.
 *
 * Image + text + table surface explaining how a coding agent uses Open
 * Design to turn a prompt into a clickable, multi-screen prototype. First of
 * the Solution use-case pages; the layout here is the template the other ten
 * (5 use cases + 5 roles) copy. Copy lives in `solution-pages-i18n.ts`
 * (English + Simplified Chinese; other locales fall back to English).
 *
 * Illustrations are AI-generated, warm-paper editorial style, hosted on the
 * marketing R2 bucket (static.open-design.ai) under
 * `landing/assets/solutions/prototype/` and served through the same
 * `cdn-cgi/image` pipeline as the homepage hero.
 */
import Layout from '../../../_components/sub-page-layout.astro';
import LazyImg from '../../../_components/lazy-img.astro';
import { getInfoPageCopy } from '../../../info-page-i18n';
import { getSolutionPageCopy } from '../../../solution-pages-i18n';
import { imageAsset, imageAssetSrcset } from '../../../image-assets';
import { localeFromPath, localizedHref } from '../../../i18n';

const SITE = Astro.site?.toString() ?? 'https://open-design.ai/';
const REPO = 'https://github.com/nexu-io/open-design';
const REPO_RELEASES = `${REPO}/releases`;
const locale = localeFromPath(Astro.url.pathname);
const href = (path: string) => localizedHref(path, locale);
const common = getInfoPageCopy(locale).common;
const page = getSolutionPageCopy(locale, 'prototype');

const title = page.title;
const description = page.description;

// Illustrations (warm-paper editorial). hero is the LCP element; the rest
// lazy-load. Names are relative to the `landing/assets/` R2 prefix.
const heroImg = imageAsset('solutions/prototype/hero.png', { width: 1536, quality: 82 });
const heroSrcset = imageAssetSrcset('solutions/prototype/hero.png', [768, 1280, 1536]);
const flowImg = imageAsset('solutions/prototype/flow.png', { width: 1536, quality: 82 });
const flowSrcset = imageAssetSrcset('solutions/prototype/flow.png', [768, 1280, 1536]);
const exampleImg = imageAsset('solutions/prototype/example.png', { width: 1280, quality: 82 });
const exampleSrcset = imageAssetSrcset('solutions/prototype/example.png', [768, 1280]);

const faq = page.faq;

const jsonLd = [
  {
    '@context': 'https://schema.org',
    '@type': 'BreadcrumbList',
    itemListElement: [
      { '@type': 'ListItem', position: 1, name: 'Open Design', item: SITE },
      { '@type': 'ListItem', position: 2, name: page.breadcrumb, item: `${SITE}solutions/prototype/` },
    ],
  },
  {
    '@context': 'https://schema.org',
    '@type': 'WebPage',
    name: title,
    description,
    url: `${SITE}solutions/prototype/`,
    isPartOf: { '@id': `${SITE}#website` },
  },
  {
    '@context': 'https://schema.org',
    '@type': 'FAQPage',
    mainEntity: faq.map(({ q, a }) => ({
      '@type': 'Question',
      name: q,
      acceptedAnswer: { '@type': 'Answer', text: a },
    })),
  },
];
---

<Layout title={title} description={description} active="solution" jsonLd={jsonLd}>
  <nav class="breadcrumb" aria-label={common.breadcrumbAria}>
    <a href={href('/')}>Open Design</a>
    <span>/</span>
    <span aria-current="page">{page.breadcrumb}</span>
  </nav>

  <article class="info-page solution-page">
    <header class="catalog-head">
      <span class="label">{page.label}</span>
      <h1 class="display">{page.heading}</h1>
      <p class="lead">{page.lead}</p>
    </header>

    <figure class="solution-hero">
      <LazyImg
        src={heroImg}
        srcset={heroSrcset}
        sizes="(max-width: 920px) 100vw, 920px"
        alt={page.heroImageAlt}
        loading="priority"
        width={1536}
        height={1024}
      />
    </figure>

    <div class="tldr-card">
      <h3>{page.tldrTitle}</h3>
      <p>{page.tldrBody}</p>
    </div>

    <section class="info-section" id="how">
      <h2>{page.stepsTitle}</h2>
      <ol class="solution-steps">
        {page.steps.map((step, idx) => (
          <li class="solution-step">
            <span class="solution-step-index">{String(idx + 1).padStart(2, '0')}</span>
            <div class="solution-step-text">
              <h3>{step.title}</h3>
              <p>{step.body}</p>
            </div>
          </li>
        ))}
      </ol>
      <figure class="solution-figure">
        <LazyImg
          src={flowImg}
          srcset={flowSrcset}
          sizes="(max-width: 920px) 100vw, 920px"
          alt={page.steps.map((s) => s.imageAlt).join('. ')}
          width={1536}
          height={1024}
        />
      </figure>
    </section>

    <section class="info-section" id="compare">
      <h2>{page.tableTitle}</h2>
      <div class="compare-table-wrap">
        <table class="compare-table">
          <thead>
            <tr>
              <th>{page.tableColCapability}</th>
              <th>{page.tableColWithOd}</th>
              <th>{page.tableColWithout}</th>
            </tr>
          </thead>
          <tbody>
            {page.tableRows.map((row) => (
              <tr>
                <th scope="row">{row.capability}</th>
                <td class="yes"><span class="check">●</span>{row.withOd}</td>
                <td>{row.without}</td>
              </tr>
            ))}
          </tbody>
        </table>
      </div>
    </section>

    <section class="info-section" id="example">
      <h2>{page.exampleTitle}</h2>
      <p>{page.exampleBody}</p>
      <figure class="solution-figure">
        <LazyImg
          src={exampleImg}
          srcset={exampleSrcset}
          sizes="(max-width: 920px) 100vw, 920px"
          alt={page.exampleImageAlt}
          width={1280}
          height={853}
        />
      </figure>
      <p>
        <a class="inline-link" href={href(page.exampleHref)}>{page.exampleLinkLabel} →</a>
      </p>
    </section>

    <section class="info-section" id="faq">
      <h2>{page.faqTitle}</h2>
      <ol class="faq-list">
        {faq.map(({ q, a }, idx) => (
          <li class="faq-item">
            <details>
              <summary>
                <span class="faq-index">{String(idx + 1).padStart(2, '0')}</span>
                <span class="faq-q">{q}</span>
                <span class="faq-toggle" aria-hidden="true">+</span>
              </summary>
              <p class="faq-a">{a}</p>
            </details>
          </li>
        ))}
      </ol>
    </section>

    <section class="info-cta" aria-label="Open Design call to action">
      <div>
        <h2>{page.ctaTitle}</h2>
        <p>{page.ctaBody}</p>
      </div>
      <div class="info-cta-actions">
        <a class="btn btn-primary" href={REPO} target="_blank" rel="noreferrer noopener">{common.starOnGithub}</a>
        <a class="btn btn-ghost" href={href('/quickstart/')}>{common.quickstart}</a>
        <a class="btn btn-ghost" href={REPO_RELEASES} target="_blank" rel="noreferrer noopener">{common.downloadDesktop}</a>
      </div>
      <div class="info-cta-meta">
        <span class="stamp">● {common.apache}</span>
        <span>{common.localFirst} · {common.byok}</span>
        <span>{common.macWinLinux}</span>
      </div>
    </section>
  </article>
</Layout>
